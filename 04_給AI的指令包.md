# 給 AI 的指令包｜把事情交給 AI 做

> 用法：下面每段是一個「咒語」。**整段複製**，貼進你正在用的 AI（領隊洄瀾＝agy、隊員太魯閣＝codex，桌面 app 或終端機都行），按 Enter，請它幫你做。
> 咒語裡的「洄瀾／太魯閣」是預設名，你改過就跟著換。**你（學生）＝小隊長。**

---

## 咒語 1：讓領隊幫我把環境準備好（裝隊員 Codex）

> 貼給**領隊洄瀾（agy）**。

```
你是領隊。請幫我把「AI 雙人小隊」需要的環境準備好：
1. 檢查這台電腦有沒有裝 OpenAI Codex，沒有就幫我用官方獨立安裝器裝起來。
2. 把這個專案資料夾的完整路徑印出來給我，等一下隊員要開同一個資料夾。
每一步用白話告訴我你在做什麼；遇到要我登入帳號或做決定時，停下來告訴我。
全程繁體中文、台灣用語。
```

---

## 咒語 2：讓 AI 訪談我、設計我的小隊 ⭐

> 貼給**領隊洄瀾**。

```
我想設計一個我自己的 AI 雙人小隊。請你「一題一題問我」，問完幫我把設定寫進這個專案資料夾的
AGENTS.md、領隊人格檔、隊員人格檔：
1. 領隊要叫什麼名字、什麼個性？
2. 隊員要叫什麼名字、什麼個性？
3. 我最想用這個小隊做什麼（例如排旅遊行程、做報告、整理筆記）？
規則：跑在 agy 上的是領隊、跑在 codex 上的是隊員；兩人靠 workspace 底下的 inbox、outbox、shared
傳紙條；預設「手動接力」模式（可切「全自動」）；全程繁體中文、自然的台灣口語。
我若答「都好」，就用預設：領隊洄瀾、隊員太魯閣。先問我第一題。
```

---

## 咒語 3：幫我把專案資料夾從零建起來（沒拿到範本時）

```
請在目前這個資料夾裡，建立一個 AI 雙人小隊的骨架：
一份 AGENTS.md（寫明：跑在 agy 上的是領隊、跑在 codex 上的是隊員，兩人靠 workspace/inbox、outbox、
shared 傳紙條；預設「手動接力」模式、可切「全自動」；全程台灣用語），一份領隊人格檔、一份隊員人格檔，
以及 workspace/inbox、workspace/outbox、workspace/shared 三個資料夾。
建好後把這個資料夾的完整路徑印出來，並用白話告訴我每個檔案做什麼。繁體中文台灣用語。
```

---

## 咒語 4：報到＋印出專案資料夾路徑（兩個 AI 各貼一次）

```
請先讀這個資料夾的 AGENTS.md，認出你是領隊還是隊員，跟我（小隊長）報到一句，
並把這個專案資料夾的完整路徑印出來給我，讓我確認兩個 AI 對著同一個資料夾。
```

---

## 咒語 5：選模式 ＋ 下第一個任務（給領隊）

```
這次用「手動接力」模式（你拆好任務寫進 workspace/inbox 後就停下來，叫我去請隊員開工，
不要自己叫隊員）。任務：幫我做一份花蓮一日遊景點清單，三個點、每點一句，要好看清楚。
請先把任務拆好寫進 workspace/inbox，再告訴我下一步去叫誰。全程繁體中文。
```
> 想省事？把第一行改成「這次用**全自動**模式（你自己叫隊員做完、收回檢查、直接回報我）」。

---

## 咒語 6：叫隊員開工（手動接力模式時，貼給隊員）

```
你是隊員。請看 workspace/inbox 有沒有領隊派的任務；有的話照著做，
做完把成品放 workspace/outbox 並留一行交接，然後叫我回去請領隊檢查。先回我一句你準備好了。
```

---

## 咒語 7：成品出來了，幫我檢查（給領隊）

```
隊員已經把成品放在 workspace/outbox 了。請你（領隊）打開來檢查：
有沒有照需求做、哪裡可以更好，需要的話幫我潤飾，最後用三句話跟我總結。繁體中文。
```

---

## 咒語 8：兩邊對不上（隊員看不到領隊的紙條）

```
我想讓兩個 AI 用同一個專案資料夾協作，但隊員看不到領隊放的檔案。
請幫我診斷：把你現在的專案資料夾完整路徑印出來，
再告訴我怎麼確認另一邊是不是同一個資料夾、以及怎麼對齊。繁體中文、一步一步、像對長輩說話。
```

---

## 咒語 9：高階進階玩法——用 Computer Use 自動跑手動接力（演示手動模式自動化）⭐

> 貼給**支援電腦操作功能（Computer Use / GUI 畫面控制）的高階 AI**（例如支援桌面操作的 Codex 或相關 Agent）。
> 這條咒語能示範如何讓 AI 扮演「學生的雙手」，在手動模式下自己切換視窗、貼指令並完成完整的手動接力驗收！

```
請用 Computer Use 幫我操作 Antigravity 視窗，完成一次手動接力流程。

分工：
- Codex (你)：拆任務、寫派工紙條、最後檢查。
- Antigravity：讀取派工紙條、完成成品、寫入 outbox。

任務主題：台北親子半日遊

流程：
1. Codex 先把任務拆好，寫入 workspace/inbox/task.md。
2. Codex 再用 Computer Use 操作 Antigravity 視窗，把以下指令貼進 Antigravity 並送出：
   「請讀取 workspace/inbox/task.md，照任務完成成品，放進 workspace/outbox/，並留下 workspace/outbox/handoff.md 交接說明。完成後請停下來，只回報『已完成，可請 Codex 檢查』。」
3. 等 Antigravity 完成後，Codex 讀取 workspace/outbox 的成品與 handoff.md。
4. Codex 檢查成品是否符合 workspace/inbox/task.md 的規格。
5. Codex 最後把審查結果回報給我。

交付格式：Markdown 表格
表格欄位包含：時間、地點、附近捷運站、活動內容、適合年齡、雨天備案、資料來源

輸出檔案：
- workspace/outbox/parent-child-halfday.md
- workspace/outbox/handoff.md
```

---

【洄瀾草稿】請 Ted 試貼幾條確認 agy／codex 都吃得動，再發給學生。特別感謝 Ted 提供「咒語 9」作為展示高階 Computer Use 點對點視窗串接的示範教材！
