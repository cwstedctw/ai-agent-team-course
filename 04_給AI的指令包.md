# 給 AI 的指令包｜把事情交給 AI 做

> 用法：下面每一段是一個「咒語」。**把整段文字複製**，貼進你正在用的 AI（`agy` 或 `codex`，桌面 app 或終端機都行），按 Enter，請它幫你做。
> 適合完全不想碰指令、只想把事情交給 AI 的同學。咒語裡的「洄瀾／太魯閣」是預設名字，你改過就跟著換。

---

## 咒語 1：讓 AI 幫我檢查環境、缺什麼幫我裝

```
請幫我把「AI 雙人小隊」需要的環境準備好。請依序：
1. 執行 agy --version、codex --version、node -v，告訴我哪個有、哪個沒有。
2. 如果沒有 Node.js 或版本低於 22，幫我裝最新版（Node.js 22 以上）。
3. 如果沒有 Codex，幫我用 npm 安裝 @openai/codex。
每一步用白話說你在做什麼；遇到要我自己登入帳號或做決定的地方，停下來告訴我。
全程繁體中文台灣用語。
```

---

## 咒語 2：讓 AI 訪談我、幫我設計小隊 ⭐

```
我想設計一個我自己的 AI 雙人小隊。請你「一題一題問我」以下事情，
問完之後幫我把設定寫進這個資料夾的 AGENTS.md、隊長人格檔、隊員人格檔：
1. 隊長要叫什麼名字、什麼個性？
2. 隊員要叫什麼名字、什麼個性？
3. 我最想用這個小隊做什麼（例如排旅遊行程、做報告、整理筆記）？
規則：跑在 agy 上的是隊長、跑在 codex 上的是隊員；兩人靠 workspace 底下的
inbox、outbox、shared 三個資料夾傳紙條協作；全程繁體中文台灣用語。
我若答「都好」，就用預設：隊長叫洄瀾、隊員叫太魯閣。先問我第一題。
```

---

## 咒語 3：幫我把團隊資料夾從零建起來（沒拿到範本時）

```
請在目前這個資料夾裡，建立一個 AI 雙人小隊的骨架：
一份 AGENTS.md（寫明：跑在 agy 上的是隊長、跑在 codex 上的是隊員，
兩人靠 workspace/inbox、outbox、shared 三個資料夾傳紙條協作，全程台灣用語），
一份隊長人格檔、一份隊員人格檔，
以及 workspace/inbox、workspace/outbox、workspace/shared 三個資料夾。
建好後用白話告訴我每個檔案做什麼。繁體中文台灣用語。
```

---

## 咒語 4：啟動隊長（貼給 agy 那個視窗）

```
你是隊長。請先讀這個資料夾的 AGENTS.md 認識團隊規則與你的角色。
之後我給你的需求，你負責想清楚、拆成清楚的任務寫進 workspace/inbox，
再告訴我去隊員那邊請他開工。先回我一句確認你準備好了。
```
> 提醒：啟動 agy 時要用 `agy --add-dir .`，它才看得到這個資料夾的收件匣、寄件匣。

---

## 咒語 5：啟動隊員（貼給 codex 那個視窗）

```
你是隊員。請先讀這個資料夾的 AGENTS.md，
然後看 workspace/inbox 有沒有隊長派的任務；有的話就動手做，
做完把成品放 workspace/outbox 並留一行交接給隊長。先回我一句確認你準備好了。
```

---

## 咒語 6：兩邊對不上（隊員看不到隊長的紙條）

```
我想讓兩個 AI 用同一個資料夾協作，但隊員看不到隊長放的檔案。
請幫我診斷：先告訴我你現在在哪個資料夾（執行 pwd），
再告訴我怎麼確認另一邊是不是在同一個資料夾、以及怎麼對齊。
（如果你是隊長 agy，也檢查我啟動時有沒有加 --add-dir .）
用繁體中文、一步一步、像對長輩說話。
```

---

## 咒語 7：下一個完整任務（直接給隊長）

```
幫我做一份花蓮一日遊景點清單，三個點、每點一句，要好看清楚。
請你（隊長）先把這件事拆成清楚的小任務寫進 workspace/inbox，
告訴我接下來該去隊員那邊叫他做什麼。全程繁體中文。
```

---

## 咒語 8：成品出來了，幫我檢查（直接給隊長）

```
隊員已經把成品放在 workspace/outbox 了。
請你（隊長）打開來檢查：有沒有照需求做、哪裡可以更好，
需要的話幫我潤飾，最後用三句話跟我總結這次成果。繁體中文。
```

---

【洄瀾草稿】請 Ted 試貼幾條確認 agy／codex 都吃得動，再發給學生。
