# 安裝設定指南｜把你的 AI 雙人小隊裝起來

> 給東華大學通識課的同學。**不用寫程式**，而且這版**以桌面 app（滑鼠操作）為主**——像平常用 app 一樣點一點就好。
> 精神：**能交給 AI 做的就交給 AI 做**（設定資料夾、甚至幫你設計角色）。你只負責「登入自己的帳號」和「下指令」。
> 全程約 **20–30 分鐘**。桌面步驟經隊員/領隊查官方文件確認過。

---

## 最快的開始方式：讓 AI 讀這份指南、帶你裝

- **方式一（最省事）：讓 AI 讀整包教材、手把手帶你。**
  把這門課的 GitHub 網址（老師會給）或這個資料夾交給你手邊任何一個 AI，跟它說：
  > 「請讀這個 repo 裡的 `02_安裝設定指南.md` 和 `AGENTS.md`，一步一步幫我把『AI 雙人小隊』裝起來。需要我登入帳號或做決定時，停下來問我。全程繁體中文台灣用語。」
  > 注意：網頁版 AI（ChatGPT／Gemini）沒有手、不能直接操作你的電腦，它會當「導航」，把該按的、該貼的告訴你，你照著點就好。
- **方式二：自己照下面步驟做。**

---

## 核心觀念：一個「專案資料夾」，兩個夥伴都開它 🔑

你的小隊住在**一個資料夾**裡——這就是你的「**專案資料夾**」。
**領隊和隊員都要打開「同一個這個資料夾」**，才傳得到紙條、才合作得起來。**全班最常卡的就是兩邊開到不同資料夾。**

### 📂 「我的小隊」資料夾怎麼來？——交給領隊 Codex 建
別擔心怎麼下載、怎麼複製、怎麼改名。等一下你**裝好領隊 Codex、登入**，**貼一句話（咒語 0）給它**，它就會自動幫你：下載這門課的教材、建好你桌面的「**我的小隊**」資料夾、檢查環境，最後把資料夾路徑印給你。你只要照 **Part A** 做。

> 這就是這門課的精神：**能交給 AI 做的就交給 AI 做。** 你負責登入帳號、下指令；繁瑣的下載與設定，交給領隊。

---

# Part A：先請領隊就位、幫你把小隊建起來（Codex app）

> 這門課裡 **Codex 是領隊洄瀾**——它能「電腦操作」、也能幫你跑下載與設定，所以讓它先就位、帶起整個小隊。

1. 裝 **Codex app**（官方桌面版），打開後**用你自己的 ChatGPT 帳號登入**（登入你自己來）。
2. 先讓它開**你的桌面**當臨時工作區：點 **Select a project／選擇專案資料夾**，選「**桌面 Desktop**」（等一下小隊建好再換過去）。
3. **貼「咒語 0」**（在 `04_給AI的指令包.md`）給它、送出。它會自動：用 `gh repo clone cwstedctw/ai-agent-team-course` 下載教材、把裡面的 `範本團隊` 複製成你桌面的 **`我的小隊`**、檢查環境（含 Antigravity 的 `agy`），最後印出 **`我的小隊`** 的完整路徑。
   > 過程中要你登入或做決定，它會停下來問你。若它說你電腦沒有 `gh`，照它的話做（它會改用 `git clone` 或教你裝）。**桌面版 Antigravity 還是你自己裝**（Part C），Codex 只幫你把專案建好。
4. **關掉 Codex、再打開一次**，這次 **Select a project 選剛建好的 `我的小隊`**。
   > ⚠️ **Windows 防呆**：點「Select a project」彈出的「選擇資料夾」視窗**只顯示資料夾、把檔案（如 `AGENTS.md`）藏起來**。正常！確認選中 **`我的小隊`**，點右下角 **「選擇資料夾」** 即可。
5. 確認畫面顯示 **Local**；對它說一句（咒語 1）：`請先讀這個資料夾的 AGENTS.md，告訴我你的身分。` → 它會認出自己是**領隊洄瀾**，並印出專案資料夾路徑。

> 🛠️ 進階（會用終端機的人才需要）：也可以在 `我的小隊` 裡直接跑 `codex`。一般同學用桌面 app 就好。

---

# Part B：讓領隊設計你的小隊

在 Codex（領隊洄瀾）裡，貼這句（複製 `04` 咒語 2），它會一題一題問你（名字、個性、想做什麼），幫你寫好 `AGENTS.md` 與人格檔，並把專案資料夾路徑印出來。

**懶得想就每題答「都好」，用預設：領隊洄瀾、隊員太魯閣。**

> 把「設計小隊」交給領隊問你，就是「讓 AI 幫你設定」的精神。

---

# Part C：讓隊員就位（Antigravity 桌面版 開「同一個」專案資料夾）

> 這門課裡 **Antigravity 是隊員太魯閣**。

1. 裝 **Antigravity 桌面版**：到 <https://antigravity.google/download> 下載安裝（認明**桌面版**，不是開發者 IDE 版）。
2. 打開它，點「**用 Google 帳號登入**」（免費 Google 帳號就行，登入你自己來）。
3. 在 Antigravity 裡**打開剛剛那個一模一樣的專案資料夾**（領隊在 Part A 印出來的 `我的小隊`）。
4. 對它說一句：`請先讀這個資料夾的 AGENTS.md，告訴我你的身分。` → 它會認出自己是**隊員太魯閣**。

> 🛠️ 進階：也可用 CLI——在 `我的小隊` 資料夾裡跑 `agy --add-dir .`。

---

# Part D：開工！選一種模式，下第一個任務

你是**小隊長**。開工前，先跟領隊講要用哪種模式：

### 模式一：手動接力（預設，看得見每一棒）
1. 跟領隊（Codex）說：`這次用手動模式。幫我做一份花蓮一日遊景點清單，三個點各一句。拆好任務寫進 inbox 後叫我去請隊員。`
2. 領隊把任務寫進 `inbox`，叫你去找隊員。
3. 你切到 **Antigravity（隊員）**：`看 inbox 接任務、做完放 outbox、叫我回去找領隊。`
4. 你回到 **Codex（領隊）**：`收 outbox、檢查、給我總結。`

🎉 你剛當了一次小隊長，親手指揮兩個 AI 接力完成一件事——全程只動嘴、沒寫半行程式。

### 模式二：全自動（省事）
跟領隊說：`這次用自動模式，幫我把整件事跑完、檢查好、直接回報我。`

### 模式三（進階示範）：領隊用 Computer Use 自己跑完手動接力 ⭐
領隊（Codex）能用「**電腦操作（Computer Use）**」當你的雙手——自己切到隊員 Antigravity 的視窗、貼指令、再回收檢查，把手動接力**自動演一遍**給你看。做法見 `04` 咒語 9。
> ⚠️ 需要較新的 Windows／Mac；執行時它會**接管你的滑鼠鍵盤、佔住前景**（你會看著它自己動）。**不是每台機器都跑得動，當示範用，不是基本盤。**

> 兩三種都玩一次，感受差別：**手動＝你掌控、看得見；自動＝快、交給 AI；Computer Use＝看 AI 替你動手。** 這就是「要不要讓人插手」——AI 時代最重要的判斷之一。

---

## 常見卡關

| 症狀 | 多半是 | 解法 |
|---|---|---|
| 隊員看不到領隊放的紙條 | 兩個 app 開到**不同資料夾** | 各自叫 AI「印出你現在的專案資料夾完整路徑」，兩邊對到一模一樣 |
| Codex（領隊）登入失敗 | 校園網路（proxy）或選錯帳號 | 換個網路再試；登入頁選你自己的 ChatGPT 帳號；被用量限制就換帳號或找老師 |
| 領隊在手動模式自己跑去做隊員的活 | 沒講清楚模式 | 開工第一句明講「用手動模式，寫完 inbox 就停、叫我去找隊員」 |
| Computer Use 跑不動 | 機器較舊或系統不支援 | 沒關係，它本來就是進階示範；改用模式一手動接力即可 |

還是卡？打開 `04_給AI的指令包.md`，把對應咒語整段貼給 AI，請它幫你診斷。

---

## 📌 給 AI 與老師看的技術備註（學生可跳過）

> 這段給「幫忙設定的 AI」和老師看——把會影響順不順的細節一次講清。學生不必懂。

**角色與環境**
- **領隊洄瀾＝Codex（跑在 codex）**；**隊員太魯閣＝Antigravity（跑在 agy）**。桌面為主：領隊用 Codex app「Select a project」開專案資料夾、隊員用 Antigravity 桌面版開**同一個**資料夾。CLI 是進階/備援。
- 兩個 agent **不直接對話**，靠專案資料夾的 `workspace/inbox`、`outbox`、`shared` 傳檔；**兩邊必須是同一個專案資料夾**。
- 三種模式：手動接力（預設，agent 不互相直呼、棒子交回小隊長）、全自動、Computer Use 示範（進階）。

**安裝與帳號**
- 領隊 Codex：官方桌面 app＋VS Code/Cursor/Windsurf 擴充（[app](https://developers.openai.com/codex/app)、[ide](https://developers.openai.com/codex/ide)），會在動工前讀 `AGENTS.md`（[agents-md](https://developers.openai.com/codex/guides/agents-md)）。用 ChatGPT 帳號登入（各方案含免費都能用、只差用量）；校園 proxy 攔 TLS 可能讓 OAuth 失敗，課前在教室網路試一次。CLI 進階：Windows `irm https://chatgpt.com/codex/install.ps1 | iex`、Mac `curl -fsSL https://chatgpt.com/codex/install.sh | sh`。
- 隊員 Antigravity：桌面版（<https://antigravity.google/download>），任何 Google 帳號（含免費）登入。CLI 進階：Windows `irm https://antigravity.google/cli/install.ps1 | iex`、Mac `curl -fsSL https://antigravity.google/cli/install.sh | bash`（裝到使用者資料夾、免系統管理員）。
- 🪟 Windows 走 CLI 時：裝完任何 CLI 要把所有終端機關掉重開，PATH 才生效（用桌面 app 不會遇到）。

**Computer Use（模式三，進階）**
- Codex app（v26.527 起）在 **Windows** 可「看畫面、點按鈕、打字」操作桌面程式（[Computer Use 文件](https://developers.openai.com/codex/app/computer-use)）。⚠️ Windows 上跑在**使用中的桌面、不能背景跑**，執行時接管前景滑鼠鍵盤；手機遠端管理那套要 Win11 build 26100＋NPU。**所以核心課程不依賴它，只當示範。**

**AGENTS.md／身分**
- codex 與 agy **都讀專案根目錄的 `AGENTS.md`**，開機自動認身分（codex app 選那個資料夾、agy 桌面版開那個資料夾即可）。沒有用 `.agents/` 子資料夾。
- 保險起見可叫 agent 第一句報到時「印出專案資料夾完整路徑」，確認兩邊同一個。

**機制說明**：兩個 agent 透過 `inbox`／`outbox` 互傳紙條的整條協作鏈，已驗證可正常運作。
