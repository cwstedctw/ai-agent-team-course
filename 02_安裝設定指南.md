# 安裝設定指南｜把你的 AI 雙人小隊裝起來

> 給東華大學通識課的同學。**不用寫程式**，看到「終端機」不要怕——它只是一個你打字、電腦回話的小黑窗。
> 這份指南的精神：**能交給 AI 做的就交給 AI 做**（裝 Node.js、設定資料夾、甚至幫你設計角色）。你只負責「登入自己的帳號」和「下指令」。
> 全程約 **20–30 分鐘**。下面每個步驟都在 Mac 上實際跑過、確認會動，照做就好，**不必試錯**。

---

## 最快的開始方式：讓 AI 讀這份指南、帶你裝

你有兩種用法，挑一種：

- **方式一（最省事，推薦）：讓 AI 讀整包教材、手把手帶你。**
  把這門課的 GitHub 網址（老師會給）或這個 `course_agent_team` 資料夾，交給你手邊任何一個 AI（agy、codex，或網頁版 ChatGPT／Gemini 都行），跟它說：
  > 「請讀這個 repo 裡的 `course_agent_team/02_安裝設定指南.md`（包含最後『給 AI 看的技術備註』），照它一步一步幫我把『AI 雙人小隊』裝起來。需要我登入帳號或做決定時，停下來問我。全程繁體中文台灣用語。」

  AI 會照這份指南幫你裝、幫你設定，你只要在它喊停時登入帳號或回答問題即可。
- **方式二：自己照下面步驟做。** 想自己動手、或手邊還沒有可用的 AI，就跟著 Part A → B → C 走。

> 🧑‍🏫 **老師注意**：要讓學生的 AI 直接「讀 repo 幫忙裝」，教材得放在學生 AI 抓得到的地方（公開 GitHub repo 或共享連結）。⚠️ 本團隊主 repo 含**私人記憶與資料**，**不要整包公開**——請另開一個**只放 `course_agent_team/`** 的公開 repo 給學生用。

---

## 開始前：三件學長姐踩過的雷，先記住

我們（洄瀾團隊）自己在 Mac 和 Windows 上跑過，最常卡的就這三個，先講在前面：

1. **🪟 Windows：裝完工具，要把所有終端機視窗「全部關掉、重開」**——不然打 `agy`／`codex` 會說「找不到指令」。這不是裝壞，是 Windows 要重開終端機才認得新指令。
2. **📁 兩個視窗一定要在「同一個團隊資料夾」**——隊長和隊員靠這個資料夾互傳紙條，不同資料夾就傳不到。
3. **🧭 啟動隊長 agy 時要加 `--add-dir .`**——這樣它才看得到你資料夾裡的收件匣、寄件匣（不加的話它會在自己的暫存區工作，看不到你的檔案）。

記住這三個，後面幾乎不會卡。

---

## 你會用到的兩個工具

| 角色 | 工具 | 你登入用的帳號 | 在哪裡跑 |
|---|---|---|---|
| **隊長**（預設叫洄瀾） | Google Antigravity，指令 `agy` | Google 帳號 | Windows／Mac 都行 |
| **隊員**（預設叫太魯閣） | OpenAI Codex，指令 `codex` | ChatGPT 帳號 | Windows／Mac 都行 |

> **不分電腦**：學校電腦教室是 Windows、你自己帶 Mac 筆電也可以。機器之間的差異（路徑、設定）大多讓 AI 幫你處理，你不用記。
> 帳號登入**一定要你自己來**——AI 不會、也不應該替你登入帳號或打密碼。

---

## 第 0 步：打開終端機

- **Windows**（電腦教室）：按開始鍵，打「PowerShell」，按 Enter。
- **Mac**（你的筆電）：按 `Command + 空白鍵`，打「Terminal」，按 Enter。

跳出來那個能打字的視窗就是終端機。所有指令打完都要**按 Enter** 才會動。

---

# Part A：裝工具（大部分交給 AI）

策略：**先裝好「隊長 agy」這一個**，因為它本身是一個會幫你做事的 AI，接著就叫它幫你把其他東西（Node.js、Codex）裝好。

## A-1　裝隊長 agy（你只跑一行）

- **Windows**（PowerShell 貼上，按 Enter）：
  ```powershell
  irm https://antigravity.google/cli/install.ps1 | iex
  ```
  裝到你自己的使用者資料夾（`%LOCALAPPDATA%`），**不需要系統管理員權限**，電腦教室也裝得起來。
- **Mac**（Terminal 貼上）：
  ```bash
  curl -fsSL https://antigravity.google/cli/install.sh | bash
  ```

裝完 **🪟 Windows 記得把終端機關掉重開**。然後驗一下：
```bash
agy --version
```
有版本號就成功。

## A-2　登入隊長（你自己登 Google）

輸入 `agy`，第一次會跳出瀏覽器要你用 **Google 帳號**登入（**免費 Google 帳號就可以**，不必付費）。登完回到終端機就能用。

## A-3　裝隊員 Codex（一行，免裝 Node.js）

Codex 有官方獨立安裝器，**不必先裝 Node.js**，一行就好：
- **Windows**（PowerShell）：
  ```powershell
  powershell -ExecutionPolicy ByPass -c "irm https://chatgpt.com/codex/install.ps1 | iex"
  ```
- **Mac**（Terminal）：
  ```bash
  curl -fsSL https://chatgpt.com/codex/install.sh | sh
  ```

裝完 **🪟 Windows 再把終端機關掉重開一次**，然後驗：
```bash
codex --version
```

> 💡 **懶得自己貼指令？讓 AI 幫你裝**：已經有 agy 在跑的話，把 `04_給AI的指令包.md` 的「咒語 1」整段貼給它，它會幫你檢查環境、缺什麼幫你裝。
> 💡 **備援**：獨立安裝器若失敗，也可以走 npm——先到 <https://nodejs.org> 裝 Node.js（LTS 版），再跑 `npm install -g @openai/codex`。

## A-4　登入隊員（你自己登 ChatGPT）

輸入 `codex`，第一次選 **Sign in with ChatGPT**，它會開瀏覽器要你用**自己的 ChatGPT 帳號**登入（這是正常的，不是中毒）。**不需要 API 金鑰。** 登完就能用。

> ChatGPT 各種方案（免費、Plus、Pro、學校 Edu…）都能用 Codex，差別只在用量限制。若用一用被限制，換個帳號或找老師。

---

# Part B：設計你自己的小隊（讓 AI 訪談你）

這是這堂課好玩的地方——**這兩個 AI 夥伴是你的，名字、個性、專長你自己決定。**

## B-1　拿到團隊資料夾骨架

跟老師拿 `範本團隊` 資料夾（已經幫你準備好 `AGENTS.md`＋兩份人格檔＋收件匣寄件匣），複製一份到桌面，改名成你喜歡的，例如 `我的小隊`。

> 懶得自己建？直接跳 B-2，讓 AI 幫你從零建好。

## B-2　讓 AI 訪談你、幫你把小隊設計出來

在隊長 agy 視窗（記得 `cd` 進你的資料夾後用 `agy --add-dir .` 啟動），貼這句：

```
我想設計一個 AI 雙人小隊。請你「一題一題問我」以下事情，問完幫我把設定寫進這個資料夾的
AGENTS.md 和兩份人格檔：
1. 隊長要叫什麼名字、是什麼個性？
2. 隊員要叫什麼名字、是什麼個性？
3. 我最想用這個小隊做什麼（例如排旅遊行程、做報告、整理筆記）？
規則：跑在 agy 上的是隊長、跑在 codex 上的是隊員；兩人靠 workspace/inbox、outbox、shared
三個資料夾傳紙條；全程繁體中文台灣用語。
我如果答「都好」，就用預設：隊長叫洄瀾、隊員叫太魯閣。
```

AI 會一題一題問你，最後幫你寫好設定。**不想花時間的人，每題都答「都好」就會用預設的洄瀾／太魯閣。**

---

# Part C：開工！讓兩個 AI 接力做事

> 👋 **跟 agent 講話，最順的方式是用桌面 app**：把你的團隊資料夾在 **Antigravity 桌面版**裡打開，直接在視窗裡跟隊長對話，比在小黑窗打字親切。下面的終端機指令做的是同一件事，是給「AI 幫你設定」時用的、也是備援——**你看不懂沒關係，照貼就會動。**
> 🔴 不管用桌面 app 還是終端機，全班最常卡的都是這個：**隊長和隊員都要對著「同一個團隊資料夾」。**

## C-1　開兩個視窗、各自進到同一個資料夾

1. 開**兩個終端機視窗**，左右並排（左＝隊長、右＝隊員）。
2. 兩個視窗各打 `cd `（**後面留一個空格**），把你的 `我的小隊` 資料夾**直接拖曳**進視窗，按 Enter。兩邊要拖**同一個**資料夾。

## C-2　啟動兩位夥伴（指令已實測會動）

- **左邊（隊長）**：
  ```bash
  agy --add-dir .
  ```
  進去後跟它說一句：`你是隊長洄瀾，先讀 AGENTS.md，之後我給你的任務你拆好寫進 workspace/inbox。`
- **右邊（隊員）**：
  ```bash
  codex
  ```
  進去後跟它說一句：`你是隊員太魯閣，先讀 AGENTS.md，看 workspace/inbox 有沒有任務。`
  > 💡 第一次它要寫檔時會問你「要不要允許」，按**同意**即可（這樣它才寫得進 outbox）。

（名字若改過，就換成你的名字。）

## C-3　三秒驗證「紙條通道」通不通

- 左邊叫隊長：`在 workspace/inbox 放一張寫著「哈囉」的小紙條。`
- 右邊叫隊員：`看看 workspace/inbox 裡有什麼，唸給我聽。`
- 隊員唸得出「哈囉」→ 通道 OK，可以開工。唸不出→八成兩個視窗不在同一個資料夾，回 C-1 核對。

## C-4　下第一個任務，看它們接力

**只對左邊的隊長說一句話**，例如：

> 「幫我做一份花蓮一日遊景點清單，三個點、各一句。任務拆好交給太魯閣，做完你檢查再給我。」

接著：
1. 隊長把任務寫進 `inbox`。
2. 你切到右邊，叫隊員「看 inbox 接任務，做完放 outbox」。
3. 隊員把成品放 `outbox`、留一行交接。
4. 回左邊，叫隊長「收 outbox 檢查、給我總結」。

🎉 你剛剛指揮了一個 AI 小隊完成一件事，全程只動嘴、沒寫半行程式。
（這個任務我們實測過：隊長真的會派工、隊員真的查了七星潭／鯉魚潭／東大門夜市寫回來、隊長真的會檢查交差。）

---

## 常見卡關（都是我們踩過的）

| 症狀 | 多半是 | 解法 |
|---|---|---|
| 打 `agy`／`codex` 說「找不到指令」 | Windows 裝完沒重開終端機（PATH 沒生效） | 把終端機**全部關掉重開**再試 |
| 隊員看不到隊長放的紙條 | 兩個視窗不在同一個資料夾 | 兩邊各跑 `pwd`（Mac）／`cd`（Windows）核對路徑一模一樣 |
| 隊長 agy 看不到 inbox/outbox | 啟動時沒加 `--add-dir .` | 關掉 agy，改用 `agy --add-dir .` 重開 |
| `codex` 裝不起來 | Node.js 太舊或沒裝 | 叫 agy 幫你裝，或自己到 nodejs.org 裝最新 LTS（要 v22 以上） |
| Codex 登入跳瀏覽器後失敗 | 多半是校園網路（proxy）或選錯帳號 | 換個網路再試；登入頁選**你自己的 ChatGPT 帳號**；若被用量限制就換帳號或找老師 |

還是卡？打開 `04_給AI的指令包.md`，把對應那段整段複製、貼給終端機裡的 AI，請它幫你診斷。

---

## 📌 給 AI 與老師看的技術備註（學生可跳過）

> 這段給「幫忙設定的 AI」和老師看——把我們（洄瀾團隊）在 Mac／Windows 實戰累積、會影響順不順的細節一次講清，照做可避開大部分試錯。學生不必懂。

**環境前提**
- 學生可能在學校 Windows 電腦教室，或自己的 Mac 筆電。日常跟 agent 溝通優先走桌面 app（Antigravity 桌面版）；終端機主要用於安裝與檔案操作。
- 兩個 agent（agy＝隊長、codex＝隊員）**不直接對話**，靠共用資料夾 `workspace/inbox`、`outbox`、`shared` 傳檔協作。

**安裝（共通）**
- 🪟 Windows 裝完任何 CLI，**PATH 不會被已開著的終端機重讀**——要把所有終端機關掉重開，否則 `agy`／`codex` 會「找不到指令」。這是 Windows 最常見的假性失敗。
- agy：Windows `irm https://antigravity.google/cli/install.ps1 | iex`（裝到 `%LOCALAPPDATA%`、**免系統管理員權限**，電腦教室可裝）；Mac `curl -fsSL https://antigravity.google/cli/install.sh | bash` 或 `brew install --cask antigravity-cli`。任何 Google 帳號（含免費）可登入，憑證進系統 keyring（Windows＝認證管理員）。
- codex：**優先用官方獨立安裝器（免 Node）**——Windows `powershell -ExecutionPolicy ByPass -c "irm https://chatgpt.com/codex/install.ps1 | iex"`、Mac `curl -fsSL https://chatgpt.com/codex/install.sh | sh`。備援走 npm：`npm install -g @openai/codex`（套件名務必是 `@openai/codex`，別裝到同名無關舊套件；npm 路線需 Node.js，套件標的是 Node ≥16）。原生 PowerShell 可跑。登入走 ChatGPT 帳號 OAuth，auth 存 `~/.codex/auth.json`。各方案（含免費）都能用 Codex、只差用量限制；⚠️ 校園網路若用 proxy 攔 TLS 可能讓 OAuth 登入失敗——課前先在教室網路試一次。

**agy 的關鍵行為（最容易踩）**
- agy 認**專案根目錄的 `AGENTS.md`**（跟 codex 一樣——我們團隊 repo 就是這樣讓 agent 開機自動認身分的，沒有用 `.agents/` 之類子資料夾）。關鍵是讓 agy 把你的團隊資料夾當成工作區：**啟動時加 `--add-dir <團隊資料夾>`**，否則它會在自己的暫存區工作、看不到你的 `AGENTS.md` 與 inbox/outbox。保險起見可在第一句話（或用 `-i` 參數）直接告訴它身分、叫它去讀 `AGENTS.md`。
- ⚠️ **`agy -p` 被「別的程式／腳本」呼叫時，回應可能不印到 stdout（拿不回它的話）**：我們在 **Windows** 上實際遇過（洄瀾、立霧去呼叫 agy 都讀不回，認證與送訊息卻正常）；Mac 的互動終端機與一般管線多半正常。**學生跟 agy 互動走桌面 app 或互動式終端機就不會碰到。** 若要讓「另一個程式自動拿到 agy 的產出」，最穩的統一寫法是叫 agy**把結果寫成檔案到絕對路徑、另一端再讀那個檔**（Mac／Windows 都通）。
- agy 的相對路徑會寫進它自己的暫存區；要它寫到指定位置，給**絕對路徑**。

**codex 的關鍵行為（比 agy 單純）**
- codex 原生讀**專案根 `AGENTS.md`**，開機自動認得身分（這點比 agy 省事）。
- codex 的回應**有**印到 stdout，可直接讀（不像 agy 非寫檔不可）。
- 要它寫檔需 workspace-write 權限；互動模式會逐步問你批准，按同意即可。
- 非互動呼叫（`codex exec`）若卡在「等待輸入」，給 stdin EOF：**Windows PowerShell** `'' | codex exec …`；**Mac zsh/bash** 用 `printf '' | codex exec …` 或 `codex exec … < /dev/null`。學生在終端機手打互動指令通常用不到這招。

**已實測可動的互傳鏈（2026-05-30，本 Mac 跑通）**
1. 隊長：`agy --add-dir <資料夾> --dangerously-skip-permissions -p "…在 inbox 建任務單…"` → 寫出 `workspace/inbox/task.md`。
2. 隊員：`codex exec -s workspace-write -C <資料夾> "讀 inbox 任務、做出來、寫 outbox、留交接"` → 寫出 `workspace/outbox/result.md`。
3. 隊長：`agy --add-dir <資料夾> -p "讀 outbox、檢查、總結"` → 回收驗收。
   學生互動版＝在兩個視窗／桌面 app 各自下這些話、由人幫忙傳一句即可。

**核心鐵則**：有 log／實際輸出／官方文件就先去讀，別憑現象腦補——我們踩過的誤判幾乎都因為沒先讀。

---

【洄瀾草稿】洄瀾統籌，安裝流程 2026-05-30 在 Mac 實測 agy↔codex 互傳全通；Windows 步驟依團隊實戰筆記整理。請 Ted 在電腦教室的 Windows 機器實跑一遍再發給學生。
