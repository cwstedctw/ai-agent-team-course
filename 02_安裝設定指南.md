# 安裝設定指南｜把你的 AI 雙人小隊裝起來

> 給東華大學通識課的同學。**不用寫程式**，而且這版**以桌面 app（滑鼠操作）為主**——像平常用 app 一樣點一點就好。
> 精神：**能交給 AI 做的就交給 AI 做**（裝工具、設定資料夾、甚至幫你設計角色）。你只負責「登入自己的帳號」和「下指令」。
> 全程約 **20–30 分鐘**。Codex 的桌面步驟由隊員立霧（Codex 本人）查官方文件確認過。

---

## 最快的開始方式：讓 AI 讀這份指南、帶你裝

- **方式一（最省事）：讓 AI 讀整包教材、手把手帶你。**
  把這門課的 GitHub 網址（老師會給）或這個資料夾交給你手邊任何一個 AI，跟它說：
  > 「請讀這個 repo 裡的 `02_安裝設定指南.md` 和 `AGENTS.md`，一步一步幫我把『AI 雙人小隊』裝起來。需要我登入帳號或做決定時，停下來問我。全程繁體中文台灣用語。」
  > 注意：網頁版 AI（ChatGPT／Gemini）沒有手、不能直接操作你的電腦，它會當「導航」，把該按的、該貼的告訴你，你照著點就好。
- **方式二：自己照下面步驟做。**

---

## 核心觀念：一個「專案資料夾」，兩個夥伴都開它 🔑

你的小隊住在**一個資料夾**裡——這就是你的「**專案資料夾**」。
**領隊和隊員都要打開「同一個這個資料夾」**，才傳得到紙條、才合作得起來。**全班最常卡的就是兩邊開到不同資料夾。**

### 📂 如何建立與使用「我的小隊」資料夾：
1. **下載教材**：執行 `gh repo clone cwstedctw/ai-agent-team-course` 將整個教材下載下來（這會自動生出一個 `ai-agent-team-course` 的資料夾）。
2. **複製並建立個人小隊**：進到剛才下載的資料夾中，找到 **`範本團隊`** 資料夾。請將它整包複製（或拖曳）到你的桌面或常用工作區，並將複製出來的資料夾重命名為 **`我的小隊`**（中文名字超好認！）。
3. **開啟同一個資料夾**：接下來，請讓 **Antigravity** 和 **Codex** 都直接開啟這個 **`我的小隊`** 資料夾。

---

# Part A：先請領隊就位（Antigravity 桌面版）

> 這門課裡 **agy 是領隊**——先讓領隊到位，再由它帶起整個小隊。

1. 裝 **Antigravity 桌面版**：到 <https://antigravity.google/download> 下載安裝（認明**桌面版**，不是開發者 IDE 版）。
2. 打開它，點「**用 Google 帳號登入**」（免費 Google 帳號就行，登入你自己來）。
3. 在 Antigravity 裡**打開你的專案資料夾**（選擇你剛剛複製並命名的 **`我的小隊`**）。

> 🛠️ 進階（會用終端機的人才需要）：也可以用 CLI——在專案資料夾啟動 `agy --add-dir .`。一般同學用桌面版就好，可跳過。

---

# Part B：讓領隊把隊員和小隊都張羅好

在 Antigravity（領隊洄瀾）裡，貼這兩句，它會幫你把後面都弄好：

**B-1　請領隊裝好隊員 Codex、印出專案資料夾路徑**（複製 `04` 咒語 1）：
```
你是領隊。請幫我把「AI 雙人小隊」需要的環境準備好：檢查有沒有裝 OpenAI Codex，
沒有就幫我用官方獨立安裝器裝起來；順便把這個專案資料夾的完整路徑印出來給我，
等一下隊員要開同一個資料夾。遇到要我登入或決定時停下來告訴我。繁體中文台灣用語。
```

**B-2　請領隊訪談你、設計你的小隊**（複製 `04` 咒語 2）：領隊會一題一題問你（名字、個性、想做什麼），幫你寫好 `AGENTS.md` 與人格檔。**懶得想就每題答「都好」，用預設：領隊洄瀾、隊員太魯閣。**

> 把「設計小隊」交給領隊問你，就是「讓 AI 幫你裝、幫你設定」的精神。

---

# Part C：讓隊員就位（Codex app 開「同一個」專案資料夾）

> 由隊員立霧（Codex 本人）查官方文件確認：Codex 有桌面 app（Mac／Windows），用滑鼠選專案資料夾、會自動讀那個資料夾的 `AGENTS.md`。

1. 裝 **Codex app**（官方桌面版），打開後**用你自己的 ChatGPT 帳號登入**。
2. 點 **Select a project／選擇專案資料夾**，用滑鼠選**剛剛那個一模一樣的專案資料夾**（即剛才建立的 **`我的小隊`**）。
   > ⚠️ **Windows 學生防呆必看小提醒**：
   > 當你在 Windows 點選「Select a project」時，彈出的視窗是「選擇資料夾」模式。在這個模式下，Windows **只會顯示資料夾，並把所有「檔案」（如 `AGENTS.md`）故意隱藏起來**。
   > 這非常正常！請不用擔心檔案不見或沒有 Clone 成功，只要確認選中的資料夾是 **`我的小隊`**，直接大膽點擊右下角的 **「選擇資料夾」** 按鈕即可！
3. 確認畫面顯示 **Local**（代表在你這台電腦的資料夾裡工作）。
4. 對它說一句：`請先讀這個資料夾的 AGENTS.md，告訴我你的身分。` → 它會自己讀到 `AGENTS.md`、認出自己是隊員太魯閣。

> 🛠️ 備援：Codex app 卡住，就在那個專案資料夾裡開終端機、輸入 `codex`（Mac 在 Finder 對資料夾開終端機；Windows 在檔案總管資料夾空白處右鍵→「在終端機開啟」）。第一次它要寫檔會問你，按**同意**。
> 各種 ChatGPT 方案（免費、Plus、Pro、學校 Edu…）都能用 Codex，差別只在用量。

---

# Part D：開工！選一種模式，下第一個任務

你是**小隊長**。開工前，先跟領隊講要用哪種模式：

### 模式一：手動接力（預設，看得見每一棒）
1. 跟領隊說：`這次用手動模式。幫我做一份花蓮一日遊景點清單，三個點各一句。拆好任務寫進 inbox 後叫我去請隊員。`
2. 領隊把任務寫進 `inbox`，叫你去找隊員。
3. 你切到 **Codex（隊員）**：`看 inbox 接任務、做完放 outbox、叫我回去找領隊。`
4. 你回到 **Antigravity（領隊）**：`收 outbox、檢查、給我總結。`

🎉 你剛當了一次小隊長，親手指揮兩個 AI 接力完成一件事——全程只動嘴、沒寫半行程式。

### 模式二：全自動（省事，但看不到隊員工作）
跟領隊說：`這次用自動模式。幫我做一份花蓮一日遊景點清單，你自己叫隊員做完、收回檢查、直接回報我。`
領隊會自己把整件事跑完、回報你。

> 兩種都玩一次，感受差別：**手動＝你掌控、看得見；自動＝快、但交給 AI。** 這就是「要不要讓人插手」——AI 時代最重要的判斷之一。

---

## 常見卡關

| 症狀 | 多半是 | 解法 |
|---|---|---|
| 隊員看不到領隊放的紙條 | 兩個 app 開到**不同資料夾** | 各自叫 AI「印出你現在的專案資料夾完整路徑」，兩邊對到一模一樣 |
| Codex 登入跳瀏覽器後失敗 | 校園網路（proxy）或選錯帳號 | 換個網路再試；登入頁選你自己的 ChatGPT 帳號；被用量限制就換帳號或找老師 |
| 領隊自己跑去叫隊員（手動模式下） | 沒講清楚模式 | 開工第一句明講「用手動模式，寫完 inbox 就停、叫我去找隊員」 |
| 打 `agy`／`codex` 說找不到指令（走 CLI 時） | Windows 裝完沒重開終端機 | 把終端機全關重開（一般用桌面 app 不會遇到） |

還是卡？打開 `04_給AI的指令包.md`，把對應咒語整段貼給 AI，請它幫你診斷。

---

## 📌 給 AI 與老師看的技術備註（學生可跳過）

> 這段給「幫忙設定的 AI」和老師看——把會影響順不順的細節一次講清。學生不必懂。

**環境前提**
- 桌面為主：領隊＝Antigravity 桌面版（開專案資料夾）、隊員＝Codex app（Select a project 選同一個專案資料夾）。CLI 是進階/備援。
- 兩個 agent **不直接對話**，靠專案資料夾的 `workspace/inbox`、`outbox`、`shared` 傳檔；**兩邊必須是同一個專案資料夾**。
- 兩種模式：手動接力（預設，agent 不准互相直呼、棒子交回小隊長）、全自動（領隊可自呼叫 codex）。

**安裝與帳號**
- agy：Antigravity 桌面版（<https://antigravity.google/download>），任何 Google 帳號（含免費）登入。CLI 進階：Windows `irm https://antigravity.google/cli/install.ps1 | iex`、Mac `curl -fsSL https://antigravity.google/cli/install.sh | bash`（裝到使用者資料夾、免系統管理員）。
- codex：有官方桌面 app＋VS Code/Cursor/Windsurf 擴充（[app](https://developers.openai.com/codex/app)、[ide](https://developers.openai.com/codex/ide)），會在動工前讀 `AGENTS.md`（[agents-md](https://developers.openai.com/codex/guides/agents-md)）。CLI 進階：官方獨立安裝器（免 Node）Windows `irm https://chatgpt.com/codex/install.ps1 | iex`、Mac `curl -fsSL https://chatgpt.com/codex/install.sh | sh`；或 npm `npm install -g @openai/codex`（套件名務必 `@openai/codex`，需 Node ≥16）。各方案含免費都能用、只差用量；校園 proxy 攔 TLS 可能讓 OAuth 失敗，課前在教室網路試一次。
- 🪟 Windows 走 CLI 時：裝完任何 CLI 要把所有終端機關掉重開，PATH 才生效（用桌面 app 不會遇到）。

**AGENTS.md／身分**
- agy 與 codex **都讀專案根目錄的 `AGENTS.md`**，開機自動認身分（agy 桌面版開那個資料夾、或 CLI `--add-dir <資料夾>`；codex app 選那個資料夾即可）。沒有用 `.agents/` 子資料夾。
- 保險起見可叫 agent 第一句報到時「印出專案資料夾完整路徑」，確認兩邊同一個。

**agy CLI 補充（只在走 CLI 時相關）**
- ⚠️ `agy -p` 被別的程式／腳本呼叫時，回應可能不印到 stdout（我們在 Windows 遇過）；學生走桌面 app 或互動終端機不會碰到。要程式化取得就叫 agy 寫檔到絕對路徑、另一端讀檔。

**已實測（2026-05-30，Mac）**：agy 寫 inbox → codex 讀做寫 outbox → agy 讀 outbox 檢查，整條互傳鏈跑通。

---

【洄瀾草稿】洄瀾統籌；agy／codex 桌面步驟經立霧查官方文件、互傳鏈經 Mac 實測。請 Ted 在電腦教室實跑一遍（尤其 Codex app 的「Select a project」畫面）再發給學生。
